# Recovery decisions

## 2026-09-16 — authorization and scope

The user asked: “Plan how to get this to working … take over deploy agents and make this work.” This authorizes execution after the earlier archaeology-only stop. The five work items remain the organizing scope, in order: W1 verification, W2 empirical measurement, W3 evidence precedence, W4 simplification, W5 current dogfood. Reproduced CLI/report defects are prerequisites of trustworthy W2 measurement, not a sixth product initiative. No runtime dependencies, version bump, new orchestrator, plugin system, or new top-level source directory are approved.

Ruling: use an isolated `codex/mattermost-recovery` worktree under `/private/tmp`; preserve the original master checkout, untracked GATES.md, and historical artifacts. Reuse the installed dependencies. The user's takeover instruction authorizes this reversible setup; no extra approval round is necessary. Decisions are owned by the root Arbiter; implementation, adversarial review, and gate evidence use separate agents.

## W1 — implementation approval; gate pending

Archaeology found that `review --generate` calls `src/agentic/runner.ts` directly (`src/cli/commands/review.ts:315`), stage 3 can mark compilation alone verified (`src/pipeline/stage3_generation.ts:292`), and the crew executor regenerates instead of executing its written artifact (`src/agents/executor.ts:30`). Fixing only the originally named files would leave the product path unverified. The actual runner accepts empty/no-report success (`src/agentic/playwright_runner.ts:192`).

Ruling: approve up to ten implementation/test files for W1: `src/agents/generator.ts`, `src/agents/executor.ts`, `src/pipeline/stage3_generation.ts`, new `src/pipeline/mutation_verification.ts`, `src/agentic/runner.ts`, `src/agentic/types.ts`, `src/agentic/playwright_runner.ts`, `src/cli/commands/review.ts`, `test/agentic_runner.test.ts`, and `test/playwright_runner.test.ts`. This is an explicit four-file budget overrun because the reachable command and its execution evidence otherwise bypass the requested guarantee. Required plan, decision, and gate records are not implementation files. Delete duplicate/invalid verification logic while adding the minimum shared helper; W4 must reduce the resulting product/documentation surface.

Ruling: require real local Playwright execution of a spec written through production generation using a clearly labeled deterministic provider fixture. This validates generation-to-verification plumbing and real clean/mutated execution without paid-provider access or nondeterministic model output. It does not validate live-model quality. The good spec must pass clean, fail with a changed-line mutant, and pass after restoration. An empty-body spec must remain unverified. Syntax/load/process/timeouts, empty/skipped reports, missing source/base, and unsupported mutations cannot count as kills. Only invert-condition, return-null, and comment-statement operators are approved. No original Mattermost checkout may be mutated during implementation/gates.

W1 remains open until an independent reviewer accepts the change and the Evidence Officer records actual stdout, exit codes, and generated artifacts in `gates/w1.txt`. No implementation self-certification closes an item.

## W2–W5 — pending detailed approvals

The fixed 30-PR sample and recorded CI evidence from `/private/tmp/impact-gate-pr-validation-2026-09-16` are immutable baseline inputs. Do not choose a different sample to improve results. Ordinary reporting repairs must not quietly change selection policy. Measured traceability must never be fabricated from passing statuses, file proximity, or Git co-change. Useful ranked recommendations and authorization to omit a full-suite test are separate claims. Missing measurements remain unknown. Each later item's exact file scope and acceptance evidence will be approved here before its implementation begins.

### W1 caller-completeness amendment

Root caller audit also found `src/cli/commands/generate.ts:73` and `src/pipeline/orchestrator.ts:148` invoke the repaired paths while already holding source root/base context. Ruling: approve those two files only for passing the existing context through and accurately displaying unverified results. The W1 implementation/test cap is now twelve named files. Leaving known callers without available context would make them unnecessarily unverified after this repair. No new command/flag or orchestrator is approved.

### W1 independent-review safety amendment

The reviewer reproduced a baseline hook replacing the disposable mutation target with a symlink; the unchecked write at `src/pipeline/mutation_verification.ts:163` followed it into the fixture's original source before the restoration check at line 168 rejected it. Separately, `src/pipeline/stage4_heal.ts:96–101` sends every written generated artifact to the legacy healer, reached through both pipeline (`src/pipeline/orchestrator.ts:159–172`) and crew (`src/agents/healer.ts:19–30`), which can invalidate verified bytes after acceptance.

Ruling: guard mutation writes before opening/writing the target and prove original fixture bytes remain unchanged in the reproduced hook case. Approve exactly two additional files, `src/pipeline/stage4_heal.ts` and `test/stage4_heal.test.ts`, raising W1's cap to fourteen product/test files. Prevent artifacts carrying the new verification evidence from entering the later legacy healer, including overlapping report/explicit targets; do not add another verifier or expand healing. This preserves the evidence for generated files and removes duplicate automatic execution. The cost is that these generated artifacts cannot be auto-healed in that same run; they must be regenerated and verified again. Both findings block W1 closure until independently re-reviewed and gated.

W1 review round 1 also reproduced quarantine failure leaving generated code discoverable (`mutation_verification.ts:49–60`) and acceptance after the restored copied source changed (`:175–181`). Ruling: restoring/removing owned generated bytes must not depend on successfully saving quarantine; validate source/artifact bytes after execution, including the crew executor's final run. Existing approved files cover both fixes. Review additionally identified the need to test whether an empty `add_scenarios` addition can borrow a preexisting test's mutation kill. If confirmed, leave that addition explicitly unverified and restore the original rather than building attribution machinery in W1. No additional file, operator, or dependency is approved for this case.

The additional attribution failure is confirmed in the independent reviewer's retained `/private/tmp/w1-adversarial-addition-LTLgW0/result.json`: stage 3 reports verified=true for an assertion-free addition, with two baseline passes but the sole mutant failure titled “preexisting boundary assertion.” Root inspected the actual artifact after the reviewer was interrupted. The conservative existing-spec-addition ruling therefore applies.

W1 implementation detail ruling: the internal mutation helper may hold an open regular-file descriptor plus path identities across execution and reuse that same concrete guard in the existing crew executor. This is necessary to prevent the reproduced pathname replacement from redirecting verifier writes and to compare the exact executed bytes after runs. It is not a new agent, orchestrator, selectable strategy, public package API, or general mutation framework. No additional operators or runtime dependencies are allowed; the Simplifier must assess whether this guard can be smaller without losing the preserved-file guarantees.

## W1 — closed after independent acceptance

W1 is accepted: `gates/w1.txt` was written by the independent Evidence Officer after a fresh full `npm test` (529 passed, zero failed), lint and unchanged product/test hashes. The good and stage-3 generated fixtures show actual Playwright clean/mutant/restored exits 0/1/0; the empty fixture remains unverified with 0/0/0 and an undiscoverable proposal. Independent re-review accepted every reported preservation, attribution and stale-evidence fix. The Simplifier accepted the change: +54 net production lines after deleting 456 existing lines, plus 335 regression-test lines. Only direct local source imports are supported; existing-spec additions, browser/remote/prebuilt applications and missing evidence stay unverified. Provider responses were deterministic fixture data; no live model or Mattermost browser run was claimed.

Ruling: retain the small audit snapshots and concrete descriptor guard because the acceptance record and preservation tests depend on them. Defer minor redundant statements. Reject extracting another shared generation lifecycle during W4: that item's frozen scope permits deletions and README edits, not a new refactor. The two remaining public generation wrappers will be assessed for actual reachability in W4; W1's verified behavior is not to be quietly reopened.

## W2 — reporting prerequisites and frozen measurement approved; gate pending

The archaeology and historical replay reproduced invalid JSON banners (`src/cli/commands/review.ts:56`), invalid refs treated as empty diffs (`:61–67`), self-created calibration artifacts (`:120–135`), computed fields omitted by JSON/Markdown (`src/engine/review_formatter.ts:217–298`), repository-relative test paths joined under the test root (`src/engine/behavior_analyzer.ts:322`), and externally targeted auto-detected roots resolved against the caller (`src/cli/defaults.ts:188`). A script over those outputs cannot reliably measure the actual recommendations. This is the previously approved W2 prerequisite deviation, not a sixth item.

Ruling: approve exactly five product files (`src/cli/commands/review.ts`, `src/cli/defaults.ts`, `src/engine/review_formatter.ts`, `src/engine/behavior_analyzer.ts`, `src/engine/plan_builder.ts`), two test files (new `test/review.test.ts`, existing `test/plan_builder.test.ts`), and three measurement files (`scripts/mattermost-replay.py`, `scripts/mattermost-replay-fixture.json`, `scripts/mattermost-replay.md`). This explicitly exceeds the original scripts-only scope by seven prerequisites. Remove unsupported coverage wording, but keep mapping/selection policy unchanged. Preserve W1 verification and context propagation. No provider, dependency, command, config schema, or public abstraction is added. Existing unrelated-test decision-softening remains a disclosed heuristic limitation; it is not silently changed as reporting work.

The exact thirty PRs in `/private/tmp/impact-gate-pr-validation-2026-09-16/prs.json` and exact-head CI observations in `ci/validated-index.json` are immutable input sources. Keep their SHA-256 provenance. Run the complete base-to-head diff against the base test inventory, not an empty parent diff and not HEAD-only test inventory. Count named existing E2E recommendations from the review separately from effective full-suite fallback. Freeze path normalization, eligibility and field extraction in the W2 harness before W3. Only terminal failed preexisting specs with exact head/run/attempt/suite identity and complete corresponding worker-report evidence enter the failure denominator; retry survivors, skipped/absent suites and incomplete reports remain excluded/unknown. The one eligible recorded failure is not established as caused by its PR. Both omission fractions and denominators must be shown; full fallback is not selection savings. Do not change sample or invent source coverage to improve the result.

W2 implementer plan approved: the five-step plan in `.superpowers/sdd/2026-09-16-mattermost-recovery/task-2-plan.md` matches the ten-file scope and frozen metric contract. Execute reporting regressions/repairs first, then the compact pinned replay harness. A full validation after reporting is appropriate because defaults affect multiple commands; avoid repeating unchanged suites absent a new failure or edit. W2 is not closed until the independent Evidence Officer reruns the complete harness and writes its table and honest denominators to gates/w2.txt.

W2 report-shape ruling: keep the existing `prIncludedTestSummary` fields (files and scenarioCount) rather than adding a new public scenario-detail shape or structural casts. Verify actual extracted titles directly through the analyzer in regression tests; CLI JSON must show the correct files/count. The brief's “actual scenario titles” requirement concerns correct extraction, not an added JSON field. This avoids touching review_types.ts/review_synthesizer.ts and preserves the ten-file cap. The cost is that individual PR-added test titles remain in analyzer data, not the summary field.

## W2 — closed after independent acceptance

W2 is accepted. The reporting regressions were observed failing; full validation passed 541 tests and lint. Independent review then reproduced large piped JSON truncation, including threshold and enforcement exits; a focused fix preserved exit precedence and passed 30 covering tests after both large-report cases failed before the fix. Re-review accepted the repair. The independent final build replay in `gates/w2.txt` completed all thirty PRs and sixty strict-JSON reports with unchanged source/build/harness/fixture hashes: named-recommendation omission **1/1 (100%)**, effective-selection omission **0/1 (0%)**, full fallback **30/30**, and **0% static selection reduction**. Fifty-two CI suites were observed, forty-six complete; missing and incomplete evidence remains unknown. This single observed failure is not established as PR-caused and does not establish general accuracy. No live Mattermost E2E or model run is claimed.

Ruling: freeze the W2 harness and fixture unchanged for W3. The Simplifier accepted the exact ten-file diff: production +32 net lines after deleting 64, tests +151, measurement +4,418 (mostly the pinned evidence fixture). Retain the duplicate parsed report artifacts and Markdown overview/detail presentation for this comparison; deleting them now would change the accepted measurement artifacts for marginal benefit. The remaining task is to improve mapping precedence without inventing measured coverage or selection savings. W1 remains closed.

## W3 — per-path candidate precedence and honest provenance approved; gate pending

Fresh archaeology found no ordinary traceability consumer in `src/engine/impact_engine.ts:65,259–273`; real callers omit its existing config (`src/api.ts:105,123,148`, CLI review:87, impact:16, gate:36). Capture emits externally supplied coverage-map relationships and a converter label, not tested revision/suite identity (`src/agent/traceability_capture.ts:364–389`). Ingest drops that origin and counts file hits (`src/agent/traceability_ingest.ts:228–255`). Current cold-start routing ignores the supplied inventory and creates no spec associations (`src/knowledge/route_families.ts:446–483`).

Ruling: existing traceability can support **declared/unverified candidate precedence**, not a measured-coverage claim. Prefer valid exact imported test relationships over scanner candidates per source path before grouping; preserve converter/legacy origin and expose structured provenance. No current input is promoted to measured by caller claims, a current HEAD, timestamp, test status, or hit count. A positive measured-execution guarantee is explicitly unavailable because the existing data lacks that provenance. Reject stale/invalid/future dates, missing or escaping paths, and tests outside the selected inventory from candidate eligibility. Unsupported revision/suite claims never authorize test omission. This is the smallest honest version of the requested precedence; inventing an instrumentation/identity framework is rejected. The cost is that W3 can improve useful run-first advice but cannot certify measured source coverage.

Ruling: all **new declared-traceability and runtime cold-start heuristic** associations remain unassessed and require full fallback; unknown paths remain unassessed. Preserve existing explicit route-manifest policy and decisions, and preserve the separate advisory contract unchanged. Do not broaden this item into forcing every previously mapped ordinary repository to full. Ordinary report evidence may say measured coverage unavailable/zero measured edges; any execution-policy label must reflect the actual plan rather than unconditionally claiming full. Both Playwright and Cypress exact candidates may be surfaced, with validated path conventions and narrow filename/domain matches rather than hard-coded Mattermost associations.

Approve exactly twelve product files: `src/knowledge/route_families.ts`, `src/engine/impact_engine.ts`, `src/agent/traceability_ingest.ts`, `src/api.ts`, `src/cli/commands/review.ts`, `src/cli/commands/impact.ts`, `src/cli/commands/gate.ts`, `src/agent/plan.ts`, `src/engine/plan_builder.ts`, `src/engine/review_types.ts`, `src/engine/review_synthesizer.ts`, `src/engine/review_formatter.ts`; and five existing test files: `test/impact_engine.test.ts`, `test/route_families.test.ts`, `test/traceability_ingest.test.ts`, `test/review.test.ts`, `test/plan_builder.test.ts`. This is a written eleven-file overrun of the original six-file budget, necessary for actual callers, all report surfaces and integration regressions. No new production/test file, runtime dependency, schema/config/version, flag, abstraction layer or orchestrator. Capture implementation and scanner implementation stay unchanged; run their existing controls and test capture→ingest origin inside the approved ingest test. Replace the empty cold-start logic instead of adding a parallel mapper.

Implementation plan approved with those amendments. Callers must pass `gitResult.repositoryRoot || config.path`, not a nested config.path, as source root. Separate HEAD source and base test repositories remain separate; resolve prefixed test paths only from established inventory-root/repository relationships. Source presence and inventory presence do not establish common execution revision. The frozen W2 script/fixture stay byte-identical. The independent W3 gate will run that exact harness and report unchanged failure fractions if that is the result; no source edges may be invented from the historical CI statuses. Full tests/lint and a focused real PR38048 static review precede independent review and gate; no live Mattermost E2E/model execution claim is approved.

W3 reporting ruling: the new unverified candidates reach existing review text such as “fully covered” (`src/engine/review_synthesizer.ts:301`). Replace those human coverage claims with factual existing-spec association/mapping wording inside the already approved report files; preserve legacy status field names and every decision/selection predicate. Add a new-candidate CLI assertion against the misleading phrase. Otherwise the new provenance field would contradict the visible recommendation. This is W3 provenance integration, not a reopening of W2 policy.


## W3 — closed; retain candidate precedence without selection claims

W3 is accepted after independent adversarial review, a consolidated fix and scoped re-review, the unchanged-method gate in `gates/w3.txt`, and Simplifier ACCEPT. The final replay completed 30/30 PRs and 60 strict-JSON reports with 215 inner exits of 0 and 964 source/build/input hashes unchanged. Named observed-failure omission remains **1/1**, effective omission **0/1** through full fallback **30/30**, with **0% selection reduction**. Candidate-bearing PRs increased from 0 to 26/30, but positive lists range from 2–817 (median 95); broad matches are a limitation, not measured accuracy. The one eligible historical failure is not established as PR-caused. The full 554 tests preceded the two scoped fixes; final covering 61 tests, build and lint passed after observed red cases. The impossible-calendar and affirmative Cypress-coverage defects were fixed without altering selection predicates.

Ruling: retain per-path declared/unverified precedence and labeled filename/domain candidates because they restore actual configuration consumption and useful exact-inventory advice while preserving conservative fallback. Do not claim measured source coverage, better failure recall, safe selective execution or runtime savings; those remain unproven. Reject tuning the method or candidate matcher to the single known failure. Existing explicit-manifest/advisory policy stays intact. Retain the small repeated date validations and provenance rendering across distinct input/output surfaces; extracting a new shared layer is not warranted. Product diff is +82 net after 118 deletions, tests +269, within the approved 17-file scope. W4 may simplify proven-dead code and documentation, but may not reopen these semantics or the frozen W2 harness.


## W4 — deletion and README scope approved; gate pending

The caller audit found all four orchestrators live through commands, root public exports or package bins (`src/index.ts:69–120`, `package.json:29–35`); only the agentic runner executes through review generation. Deleting these layers wholesale would guess about external callers. The actual parser registers 21 main command tokens (`src/cli/parse_args.ts:212–218`), correcting the prompt's older count: review/gate are the two product commands, the remaining nineteen are experimental.

Ruling: approve the Simplifier's exact three-file plan. Delete the unused 113-line `src/pipeline/spec_verifier.ts` and only its three obsolete re-export/comment lines in `src/validation/guardrails.ts:131–133`; no root or declared package export exposes those symbols and no source/test/script consumer remains. Unsupported absolute access to installed internal files is the residual compatibility limit. Rewrite `README.md` to at most 220 lines with accurate source-checkout usage, static mapping and full-fallback limits, complete experimental command inventory, and links to real evidence. Preserve all live orchestrators, public interfaces, tests and frozen replay files. Reject incidental unused-import edits and any replacement helper/refactor. The expected reduction is at least 593 product/documentation lines; the complete item, including gate and closure records, must remain net negative. W4 is not closed until independent review and its real execution gate pass.


### Explicit W3 test follow-up discovered by W4's full gate

W4's independent full run passed 556/557 tests and exposed `test/plan_builder_ai.test.ts:235` still expecting the old “Cypress only” reason. This assertion and its producer are unchanged from W3 commit `bd92656`; the new factual wording was already reviewed and accepted. Ruling: explicitly reopen only this W3 test assertion, increasing its approved test scope by one file, and replace the stale substring with the actual associated-Cypress/no-associated-Playwright wording. Do not alter production behavior, predicates or the other assertions. Commit this test maintenance separately before the W4 deletion commit, then rerun the full suite once. W4 remains deletion/README only relative to that new base. This is a recorded integration correction, not a sixth feature or a silent reopening.


## W4 — closed after independent acceptance

W4 is accepted after independent review, the real execution gate in `gates/w4.txt`, and the Simplifier's post-gate ACCEPT. Relative to the separate W3 test follow-up `4b88357`, the three product/documentation files remove 743 net lines: 117 unused verifier/re-export lines and a README reduction from 697 to 71 lines. All four live orchestrators and all 21 command tokens remain; review/gate are the documented product and the other nineteen experimental. The independent final suite passed 557/557 with lint passing; pinned Mattermost PR38048 review exited 0 and gate correctly exited 1 for fourteen unassessed files. Source and prior inventory artifacts stayed intact. The gate does not claim Mattermost E2E execution or selective test safety. Ruling: retain the public compatibility paths and reject further deletion or a shared lifecycle refactor without evidence; no further product edits belong to W5.


## W5 — compact current evidence scope approved; gate pending

Ruling: approve the five-bullet W5 plan for only `dogfood/2026-09-16/`: fresh pinned PR38048 review/gate output and command provenance, a concise dated README, and byte-preserving copies of small W1 verification, W2/W3 replay summaries, and W4 final validation receipts. Preserve the March folder and distinguish its synthetic examples from the fixed thirty-PR comparison. Reject another full suite, a new model/Mattermost E2E run, huge clone/raw-CI copies, and product changes: final product source is `0030639` and already passed 557/557 plus lint. In response to the user's quota concern, reuse existing agents for distinct implementation/review/evidence roles, keep the final review focused on integration, and reuse unchanged-source validation. Independent artifact checks and the final integration review still precede closure and landing.


### Explicit W3 schema compatibility follow-up

Final integration review found ordinary plan output's new `mappingProvenance` and `evidence` fields missing from the exported `schemas/plan.schema.json`, whose root rejects additional properties. Ruling: approve only that schema and the existing `test/plan_builder.test.ts` for a focused compatibility repair. Add optional, typed definitions matching the already emitted fields, preserving schemaVersion 1.0.0 and advisory behavior; do not loosen additionalProperties or add a dependency/validator framework. A regression must observe the real produced plan's unknown-root-field failure before the schema fix and pass afterward. This is a public-contract correction to W3, not a W5 product expansion. Keep runtime source frozen; reuse its passing full suite plus this focused contract check and required GitHub CI. Commit separately before W5 delivery. W5 already captured the unchanged runtime at 0030639; retain that exact provenance rather than relabeling it with the schema-only commit.


The schema follow-up is accepted by the independent final integration reviewer. The exported schema now permits the already emitted optional provenance/evidence fields; its strict root, schemaVersion and advisory definitions remain unchanged. The root-key regression failed first with exactly those missing fields, then the covering 22 tests passed. This is not full JSON Schema validation. Runtime source is unchanged, so the final W4 suite remains applicable and required GitHub CI will validate the final branch. No Critical or Important integration findings remain.


## W5 — closed after independent artifact acceptance

W5 is accepted after the independent `gates/w5.txt` artifact audit, final integration approval, and a distinct Simplifier's ACCEPT. The dated 24-file, 119,922-byte bundle preserves 22 evidence hashes and thirteen byte-identical retained receipts. Fresh PR38048 review/gate exits are 0/1 with seventeen unverified candidates, fourteen unassessed files and full fallback; both pinned repositories and the inventory's four prior artifacts are preserved. The capture remains correctly attributed to runtime commit `0030639`, before the separately reviewed schema-only follow-up `40b66c8`. The final runtime suite is 557/557 with lint passing; the additional schema regression passed in its focused 22-test run. Required GitHub CI will check the assembled branch before merge.

Ruling: retain the small bundle as-is; do not repeat identical replays/tests or add another framework. The separate W2/W3 summary copies preserve provenance even where bytes match. All five requested items are closed, with no outstanding Critical or Important review finding. The conclusion remains limited: static review and conservative candidate advice work; measured source coverage, safe selective CI, improved failure recall, runtime savings, live-model quality and Mattermost browser generation remain unproven. Keep Mattermost's full CI. Landing this source does not publish an npm version.
